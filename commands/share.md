---
description: Share this session as a replayable spec
disable-model-invocation: true
allowed-tools: Bash(workers-spec:*)
---

Share the current Claude Code session as a replayable specification.

Run the following command:

```
workers-spec share ${CLAUDE_SESSION_ID}
```

Display the returned URL to the user. Suggest they can share it with others using `/run <url>`.
