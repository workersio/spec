{
  "name": "workers-spec-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy"
  },
  "dependencies": {
    "nanoid": "^5"
  },
  "devDependencies": {
    "wrangler": "^4",
    "@cloudflare/workers-types": "^4"
  }
}
